<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<title>Toko Mekar Mandiri</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<style>
:root{
  --primary:#2e7d32;
  --bg:#f6f7f9;
}
*{box-sizing:border-box}
body{
  margin:0;
  font-family:Segoe UI, Arial, sans-serif;
  background:var(--bg);
}
header{
  background:var(--primary);
  color:#fff;
  padding:16px;
  text-align:center;
}
header h2{margin:0}
header p{margin:5px 0 0;font-size:14px}

.container{
  padding:15px;
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(160px,1fr));
  gap:12px;
  padding-bottom:120px;
}

.card{
  background:#fff;
  border-radius:12px;
  padding:12px;
  box-shadow:0 4px 10px rgba(0,0,0,.08);
  display:flex;
  flex-direction:column;
  justify-content:space-between;
}
.card h4{
  margin:0 0 6px;
  font-size:15px;
}
.price{
  font-weight:bold;
  color:var(--primary);
  margin-bottom:10px;
}
.card button{
  background:var(--primary);
  border:none;
  color:#fff;
  padding:8px;
  border-radius:8px;
  cursor:pointer;
}

.cart-bar{
  position:fixed;
  bottom:0;
  left:0;
  right:0;
  background:#fff;
  border-top:2px solid var(--primary);
  padding:12px;
  box-shadow:0 -4px 10px rgba(0,0,0,.1);
}
.cart-info{
  font-size:14px;
  white-space:pre-line;
}
.total{
  font-weight:bold;
  margin-top:6px;
}
.cart-bar button{
  margin-top:8px;
  width:100%;
  padding:10px;
  background:var(--primary);
  border:none;
  color:#fff;
  border-radius:10px;
  font-size:16px;
}
select{
  width:100%;
  padding:8px;
  margin-top:6px;
  border-radius:6px;
}
</style>
</head>
<body>

<header>
  <h2>Toko Mekar Mandiri</h2>
  <p>Sembako & Klontong • Pesan via WhatsApp</p>
</header>

<div class="container" id="produkList"></div>

<div class="cart-bar">
  <div class="cart-info" id="cartText">🛒 Keranjang kosong</div>
  <div class="total" id="totalHarga">Total: Rp0</div>

  <select id="pembayaran">
    <option>Transfer Bank</option>
    <option>COD (Bayar di Tempat)</option>
    <option>QRIS</option>
  </select>

  <button onclick="checkout()">Checkout WhatsApp</button>
</div>

<script>
const whatsapp="6285162808162";

const produk=[
 {nama:"Beras Medium",harga:12000},
 {nama:"Beras Super",harga:14000},
 {nama:"Gula Pasir Curah",harga:14000},
 {nama:"Minyak Goreng",harga:16000},
 {nama:"Mie Instan",harga:3000},
 {nama:"Tepung Terigu",harga:10000},
 {nama:"Sabun Mandi",harga:5000},
 {nama:"Shampo",harga:7000},
 {nama:"Pasta Gigi",harga:9000},
 {nama:"Air Mineral",harga:4000}
];

let cart=[];

const list=document.getElementById("produkList");

produk.forEach((p,i)=>{
  const card=document.createElement("div");
  card.className="card";
  card.innerHTML=`
    <h4>${p.nama}</h4>
    <div class="price">Rp${p.harga.toLocaleString()}</div>
    <button onclick="addCart(${i})">Tambah</button>
  `;
  list.appendChild(card);
});

function addCart(i){
  cart.push(produk[i]);
  renderCart();
}

function renderCart(){
  if(cart.length===0){
    cartText.innerText="🛒 Keranjang kosong";
    totalHarga.innerText="Total: Rp0";
    return;
  }
  let text="";
  let total=0;
  cart.forEach(p=>{
    text+=`• ${p.nama}\n`;
    total+=p.harga;
  });
  cartText.innerText=text;
  totalHarga.innerText="Total: Rp"+total.toLocaleString();
}

function checkout(){
  if(cart.length===0){
    alert("Keranjang masih kosong");
    return;
  }
  let pesan="Halo Toko Mekar Mandiri, saya mau pesan:%0A";
  let total=0;
  cart.forEach(p=>{
    pesan+=`- ${p.nama} (Rp${p.harga})%0A`;
    total+=p.harga;
  });
  pesan+=`%0ATotal: Rp${total}%0APembayaran: ${pembayaran.value}`;
  window.open(`https://wa.me/${whatsapp}?text=${pesan}`,"_blank");
}
</script>

</body>
</html>